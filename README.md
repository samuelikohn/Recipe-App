amounts in ingredients need to be able to do fraction/decimals - cannot start with 0 or add .
Add directions to recipe level
add recipe scaling?
directions are numbered list, but still stored as a block of text
component ingredients are not unique, can have different prep methods
 - add client validation to prevent crash